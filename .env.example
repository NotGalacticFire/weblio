# Web3Forms access key. Create one free at https://web3forms.com (no account
# needed — it emails you the key). Without it the contact form degrades to a
# mailto: link, which still works but converts worse.
PUBLIC_WEB3FORMS_KEY=

# Address shown in the footer and used by the mailto fallback.
PUBLIC_CONTACT_EMAIL=ravi@weblio.design
